#ifndef OPERATIONS
#define OPERATIONS

void PerfTransaction(char const *accountName, char const *accountName2);
void EditInfo(char const *accountName);
void ViewInfo(char const *accountName);
void DeleteAccount(char const *accountName);

#endif